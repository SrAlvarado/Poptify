{
  "name": "poptify",
  "version": "0.1.0",
  "description": "Floating Spotify now-playing popup (iOS lock-screen style) for macOS",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "tauri": "tauri"
  },
  "dependencies": {
    "@tauri-apps/api": "^2",
    "hydra-synth": "^1.4.0"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2",
    "vite": "^5"
  }
}
